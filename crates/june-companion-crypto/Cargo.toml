[package]
name = "june-companion-crypto"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Compatibility forwarding crate for clovy-companion-crypto"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
clovy-companion-crypto = { path = "../clovy-companion-crypto" }
